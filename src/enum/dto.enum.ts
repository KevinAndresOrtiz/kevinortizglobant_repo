export enum stateRepository {
  E = 'Enable',
  D = 'Disable',
  A = 'Archived',
}

export enum statusRepository {
  A = 'Active',
  I = 'Inactive',
}

export enum stateVerification {
  VERIFICADO = 604,
  EN_ESPERA = 605,
  APROBADO = 606,
}
